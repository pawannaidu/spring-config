
server:
  port: 8080
spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: ''
  h2:
    console:
      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
  main:
    allow-circular-references: true
  config:
    import:
      - "accounts_qa.yml"
      - "accounts_prod.yml"
  profiles:
    active: "prod"
build:
  version: 3.0
  accounts:
  message: "Welcome to Accounts related local api's"
  contactDetails:
    name : "Eazybytes"
    email : "7lG5o@example.com"
  onCallSupport:
    -(555) 555-1234
    -(555) 523-1345
